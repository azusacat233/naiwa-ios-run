# 奶娃快跑 · iOS 工程
按照用户要求命名为“奶娃快跑”。默认入口在 iOS App 中直接运行指定参考网页，保留原站模型、地图、服装和游戏逻辑。另附带使用同款角色模型的离线练习版。

## 打开和安装
1. 将整个文件夹复制到 Mac，使用 Xcode 打开 NaiwaRunner.xcodeproj。
2. 在应用 Target 的 Signing & Capabilities 选择自己的 Team，并把 Bundle Identifier 改为自己的唯一标识。
3. 连接 iPhone，选择该设备，点击 Run；按设备提示完成开发者模式和签名信任。
4. 最低 iOS 16，目标为竖屏 iPhone。当前环境为 Windows，未运行 Xcode 编译和真机测试；这里交付的是源码工程，不是已签名 IPA，也没有上架。

## 默认原版模式
- 运行 https://naiwa-kuaipao.pages.dev/?v=6.3.3 ，使用持久化 WKWebView 存储，保留原站内容与操作。
- 应用显示名、网页标题和静态标题改为“奶娃快跑”。嵌入图片或原站动态生成的文字可能仍含原站名称。
- 依赖网络和原站持续可用，版本参数不保证服务器资源永远不变。没有建立独立的同款内容服务器。
- 切后台时尝试暂停奔跑及媒体；返回后手动继续。
- 原站进度保存在本应用 WebKit 数据中，与 Safari、离线练习版相互独立。

## 离线练习版
在连接原版的加载界面或连接失败界面，点击“离线练习（部分玩法）”即可进入。重新启动应用回到原版入口。
- 内置三条跑道、换道、跳跃、滑铲、三类障碍、金币、加速、战绩和音效。
- 主角、牛来、猎犬使用原站公开模型几何和原色贴图，可在首页查看三个角色。
- Three.js 显示真实三维角色，Canvas 绘制透视跑道；灯光和动作是本项目实现。
- 三种风景配色、四种角色色调、三项成就；不是原站十张地图和 72 套服装的完整离线移植。
- UserDefaults 持久化已结算进度；突然强退可能丢失未结算收益。
- 桌面可直接打开 NaiwaRunner/Web/index.html 试玩。按方向键或底部按钮操作。

## 文件
- NaiwaRunner.xcodeproj：可直接打开的 Xcode 工程，无需安装第三方 Swift 包。
- NaiwaRunner/GameScreen.swift：原版入口、离线备用入口、生命周期和存档桥。
- NaiwaRunner/Web：随 App 打包的离线游戏及角色资源。
- ASSET_SOURCES.md：模型来源和使用边界。
- VALIDATION.md：已执行验证和待执行真机检查。
- preview-*.png：离线版浏览器截图，不是 iOS 真机截图。

## 测试
工程目录运行：node --test tests/core.test.cjs
原版远程内容实际运行和生命周期仍需 iPhone 验证。

## 交付边界
默认模式满足直接运行原网页、保留相同内容的路线；完整独立离线移植未完成。原站归第三方所有，本工程不改变其权利归属，也没有确认商业发行授权。
